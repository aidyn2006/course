spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/FinalProject

logging:
  level:
    org:
      springframework:
        security: DEBUG
servlet:
  multipart:
    enabled: true
    max-file-size: 10MB
    max-request-size: 10MB

